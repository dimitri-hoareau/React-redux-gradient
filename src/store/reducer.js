const stateInitial = {
  firstColor: "#e367a4",
  lastColor: "#48b1f3",
  direction: "90deg",
  nbColors: 0,
};

const reducer = (state = stateInitial, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
